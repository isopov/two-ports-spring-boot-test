server:
  port: 9080

management:
  server:
    port: 9081

spring:
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ${GITHUB_CLIENT_ID}
            client-secret: ${GITHUB_CLIENT_SECRET}
            client-name: Github user
            provider: github
            scope: user